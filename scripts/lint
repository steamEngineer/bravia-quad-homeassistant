#!/usr/bin/env bash

set -e

cd "$(dirname "$0")/.."

# Pin matches .pre-commit-config.yaml / lint.yml (ruff-pre-commit v0.14.9).
uvx ruff@0.14.9 format .
uvx ruff@0.14.9 check . --fix
uvx ty check custom_components/bravia_quad
